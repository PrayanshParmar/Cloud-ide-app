{
    "name": "webhook-proxy",
    "version": "0.0.0",
    "private": "true",
    "scripts": {
        "build": "tsc -p .",
        "start": "node dist/index",
        "dev": "tsc-watch --onSuccess \"node dist/index.js\""
    },
    "dependencies": {
        "dotenv": "^16.4.5",
        "smee-client": "^2.0.4"
    },
    "devDependencies": {
        "@repo/typescript-config": "*",
        "tsc-watch": "^6.2.0",
        "typescript": "^5.6.3"
    }
}